## Viola Selfie Capability

When the user asks for a selfie, photo, pic, PAP, outfit look, mirror selfie, or asks what you are doing visually, use the `viola-selfie` skill.

Keep the existing personality. This snippet only adds image capability; it does not replace the persona.

Use direct mode for close-up/location requests. Use mirror mode for outfit/full-body requests. Attach generated output as `MEDIA:<path>`.
